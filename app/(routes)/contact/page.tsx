import Container from "@/components/container";
import React from "react";

const ContactPage = () => {
  return (
    <Container className="h-[65vh] flex items-center justify-center px-6">
      <div>
        <span className="capitalize font-semibold">Contact</span> page will not
        be contructed since its content would be dummy!
      </div>
    </Container>
  );
};

export default ContactPage;
